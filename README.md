# Job Portal – Backend (Spring Boot)

This is the backend service for the Job Listing Portal application built using Spring Boot, Spring Security (JWT), and MySQL.

## Tech Stack
- Java 17+
- Spring Boot
- Spring Security + JWT
- Spring Data JPA
- MySQL
- Maven

## Running the Project
mvn spring-boot:run

Server runs on http://localhost:8080
